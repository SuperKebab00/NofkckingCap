{
  "private": true,
  "scripts": {
    "format": "prettier . --write",
    "check:format": "prettier . --check",
    "test": "node tests/security-smoke-tests.mjs"
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
